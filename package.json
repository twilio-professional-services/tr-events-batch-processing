{
  "name": "tr-events-bulk-processing",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.3.0",
    "axios": "^1.6.7",
    "lodash": "^4.17.21",
    "randomstring": "^1.3.0",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.5.4"
  },
  "engines": {
    "node": "18"
  }
}
